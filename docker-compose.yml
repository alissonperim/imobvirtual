services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: imobvirtual
      POSTGRES_PASSWORD: imobvirtual
      POSTGRES_DB: imobvirtual
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
